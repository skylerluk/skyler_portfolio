// OWNED BY TRACK E. Other tracks: read-only.
// Track E fills logos + screenshots here, keyed by project id.
import type { ProjectAssets } from './types'

export const projectAssets: Record<string, ProjectAssets> = {}
